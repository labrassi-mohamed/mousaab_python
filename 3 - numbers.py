from math import sqrt

age  = "16"

# 1. Convert the variable age to an integer
age = int(age)
print(age)

# 2. Convert the variable age to a float
age = float(age)
print(age)

# Numbers methods
print(abs(-25))   # return the absolute value of a number
print(pow(2, 3))  # return the value of x to the power of y
print(max(2, 3))  # return the highest number
print(min(2, 3))  # return the lowest number
print(round(2.3)) # round a number to the nearest integer
print(round(2.8)) # round a number to the nearest integer
print(sqrt(25))   # return the square root of a number
